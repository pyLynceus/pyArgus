"""Readers for the data that arrives from the field.

Optional dependencies stay behind lazy imports with an instruction, so
the math core and QA never fail to import because a format library is
missing.
"""
