"""The math core: pure functions, no I/O, no GUI, no optional imports.

Everything in here must be testable against synthetic data with a known
answer. The Phase-4 strip-adjustment solver will live beside these
modules and linearize the forward model in ``georef``.
"""
